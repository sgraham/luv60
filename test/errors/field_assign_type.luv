# CRET: 1
# CERR: {self}:10:10:    self.data = realloc(cast *u8 self.data, sizeof(int) * 100)
# CERR: {ssss}                ^ error: Cannot assign type *u8 to field 'data' which is type *i32.
foreign *u8 realloc(*u8 ptr, u64 new_size)

struct Stuff:
    *int data

on Stuff def init(self):
    self.data = realloc(cast *u8 self.data, sizeof(int) * 100)
